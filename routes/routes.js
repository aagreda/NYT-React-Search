//Import Dependencies
import path from 'path';
import db from '../controller/connection.js';
import schema from '../models/schema.js';

//export function 
export function (app){
	//create a function to get all articles
	// function articles(req,res){
	// 	schema.find({}).then(function(articles){
	// 		res.send(articles);
	// 	}).catch(function(err){
	// 		console.log(err);
	// 	});
	// };

	//    //app.get
	//    app.get("/articles", articles);
	   
}